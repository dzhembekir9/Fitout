import StyledNavbarButton from '../../utilities/NavbarButton/StyledNavbarButton'

const NavbarButton = (props) => {
    return (
        <StyledNavbarButton>{props.text}</StyledNavbarButton>
    );
}
 
export default NavbarButton;